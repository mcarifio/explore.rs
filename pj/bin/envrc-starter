# direnv allow
export TOP=$(realpath -Lm $(dirname ${BASH_SOURCE%*/}))
PATH_add $(find ${TOP} -type d -name bin)
# source env.sh || true

for _c in rustc cargo; do $(type -P ${_c}) > /dev/null || >&2 echo "${_c} unexpectedly not on PATH"; done
